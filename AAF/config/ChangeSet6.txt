{(numOfReq;equal 1;(null,user request per second is 1);Log analysis result of  numOfReq;"")
(newUser;false;(null,false for authorized credential with previous history);Search result userInfo log;"")}
